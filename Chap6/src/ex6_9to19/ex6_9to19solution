6-9
weapon armor는 static 변수로 선언해야함. 
weaponUp, ArmorUp 메서드도 static으로 선언.

6-10
b : 생성은 new 연산자가 한다. 
e : 오버로딩 완전많이함. 생성자도 메서드의 한 종류일 뿐. 

6-11
b : 메서드 내에서만 사용가능. (클래스 멤버에는 못쓴다고 하네요... 몰랐음.)

6-12
c : 리턴 타입은 오버로드에 영향 못줌. 
d : 매개변수의 타입만 다르면 됨. 이름은 상관없음. 

6-13
b,c,d : 함수의 반환형은 달라도 상관없고, 인수의 자료형은 반드시 달라야 함. 

6-14
c : 생성자는 인스턴스 생성할때만 실행됨. 클래스 변수는 먼저 수행된다. 
e : 클래스 변수가 먼제 초기화됨. 인스턴스 변수는 생성할때 초기화. 

6-15
a : 초기화 순서 : 기본값 - 명시적초기화 - 초기화 블럭 - 생성자

6-16 a, e
a : 지역변수는 항상 초기화 해야함. 
d : 힙 영역에 저장되므로, 부담이 더 많지 않을까?(땡. 로컬스택에 쌓였다 없어지므로 부담적)
e : call stack 에 생성됨. 

6-17
b : 나머지 메서드 들은 println의 종료를 기다리며 대기탐. 

6-18
A라인 : 클래스 변수는 인스턴스 변수보다 먼저 초기화 되기 때문에 이 줄이 실행될때, iv는 초기화 되어있지 않음.
B라인 : 역시 클래스 변수를 반환하는 클래스 메서드는 인스턴스 변수 사용불가함. 
D라인 : 클래스 메서드에는 인스턴스 메서드 사용불가. 

6-19
"ABC123456"....?아닌가?
- 답을 찾아보니 아니군요.. ㅠㅠ 문자열은 수정할 수 없기때문에 새로운 공간에 복사해서 나타내는군요 ㅠㅠ





